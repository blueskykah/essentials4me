local ks = true

if ks == true then
    game.Players.LocalPlayer:Kick("Script Offline Check Back Soon")
    task.wait(1)
    while true do
    end
    repeat
    until false
    for i = math.huge, math.huge do
    end
    game.Players.LocalPlayer:Destroy()
else
    print("KS OFF RUNNING SCRIPT")
end
